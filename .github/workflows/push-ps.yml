name: onpush
on:
    push:
        branches:
            - "release/**"
            - main

        paths: 
            - "src/**"    
    
    


jobs:            
    test: 
        runs-on: ubuntu-latest
        steps: 
            - name: checkout code
              uses: actions/checkout@v6-beta

            - name: run test 
              run:   npm test
                  


